# -*- mode: python -*-
a = Analysis(['lexer_generator.py'],
             pathex=['/home/kundan/cs335a/pascal-to-mips-compiler'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='lexer_generator',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='lexer_generator')
